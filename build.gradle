allprojects {
    buildscript {
        ext.kotlin_version = '1.3.11'
        ext.konan_version = '0.9.2'

        repositories {
            jcenter()
            maven { url 'http://dl.bintray.com/kotlin/kotlin-eap' }
            maven { url 'http://dl.bintray.com/kotlin/kotlin-dev' }
            google()
        }
    }
}

repositories {
    mavenCentral()
    google()
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.10.2'
}